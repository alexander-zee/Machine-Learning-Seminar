# Ward cluster returns (generated)

After `create_cluster_portfolios`, this folder contains:

- `cluster_returns.csv` — monthly excess returns for each cluster (used by Part 2 Ward AP pruning)

The CSV is **not** committed (gitignored). This README only documents the path for new users.

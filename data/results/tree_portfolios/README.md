# Tree portfolio outputs (generated)

After `create_tree_portfolio` + `combine_trees` + `filter_tree_ports`, you will see subfolders such as:

- `LME_OP_Investment/` (example triplet LME × OP × Investment)
  - `level_all_excess_combined_filtered.csv` — wide matrix of excess returns used by Part 2 AP pruning

This directory is empty in a fresh clone until you run Part 1. Large CSVs here are **not** committed.
